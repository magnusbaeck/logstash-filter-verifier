input {
  stdin {
    id => plain
    codec => plain
    add_field => {
      "input_codec" => "plain"
    }
    tags => [ "input_codec_plain" ]
  }
}

input {
  stdin {
    id => line
    codec => line
    add_field => {
      "input_codec" => "line"
    }
    tags => [ "input_codec_line" ]
  }
}

output {
  stdout {
    id => stdout
  }
}
